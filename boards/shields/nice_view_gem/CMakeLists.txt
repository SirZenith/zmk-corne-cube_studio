if(CONFIG_ZMK_DISPLAY AND CONFIG_NICE_VIEW_WIDGET_STATUS)
    zephyr_library_include_directories(${CMAKE_SOURCE_DIR}/include)
    zephyr_library_sources(assets/images/icons.c)
    zephyr_library_sources(assets/fonts/pixel_operator_mono.c)
    zephyr_library_sources(assets/images/crystal.c)

    zephyr_library_sources(custom_status_screen.c)
    zephyr_library_sources(widgets/util.c)

    if (CONFIG_NICE_VIEW_GEM_INFO_CENTRAL)
        zephyr_library_sources(widgets/animation.c)
        zephyr_library_sources(widgets/layer.c)
    elseif(CONFIG_NICE_VIEW_GEM_INFO_PERIPHERAL)
        zephyr_library_sources(widgets/animation.c)
    endif()
endif()
